// import React from 'react'

const textContainer = ({gameTitle, gameText}) => {
    return (
        <>
            <h1>{gameTitle}</h1>
            <p>{gameText}</p>
        </>
    )
}

export default textContainer
